# Fail2ban filter para limitar peticiones HTTP
# Este filtro detecta IPs que hacen demasiadas peticiones HTTP

[Definition]

# Detectar cualquier petición HTTP (GET, POST, etc.)
failregex = ^<HOST> -.*"(GET|POST|PUT|DELETE|HEAD|PATCH|OPTIONS).*HTTP.*"

# Ignorar peticiones exitosas si solo quieres bloquear errores
# failregex = ^<HOST> -.*"(GET|POST|PUT|DELETE|HEAD|PATCH|OPTIONS).*HTTP.*" (4\d\d|5\d\d)

ignoreregex =
